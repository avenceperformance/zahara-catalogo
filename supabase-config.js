/* =========================================================================
   CONFIGURAÇÃO DO SUPABASE  ▸  preencha as 2 linhas abaixo
   ─────────────────────────────────────────────────────────────────────────
   Onde achar (no painel do Supabase):
     Settings ▸ API
     • Project URL          → cole em SUPABASE_URL
     • Project API keys ▸ anon public  → cole em SUPABASE_ANON_KEY

   A "anon key" é pública por natureza — pode ficar aqui sem problema.
   Quem protege os dados é o Row Level Security (já configurado no schema.sql).
   ========================================================================= */
const SUPABASE_URL = "https://SEU-PROJETO.supabase.co";
const SUPABASE_ANON_KEY = "COLE_AQUI_A_ANON_PUBLIC_KEY";
